apply plugin: 'maven'

uploadArchives{
    repositories {
        mavenDeployer {
            pom.groupId='com.cheney.plugin'
            pom.artifactId='auto-tracker-plugin'
            pom.version='1.0.0'
            repository(url:uri("${rootProject.projectDir}/repo"))
        }
    }
}